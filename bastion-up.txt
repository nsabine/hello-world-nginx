# These instructions are to be used in the event a bastion host is necessary over the use of Cloud Shell

gcloud auth login --no-launch-browser

sudo yum install -y git docker kubectl

DEVSHELL_PROJECT_ID=$(gcloud config get-value project)

sudo systemctl start docker
